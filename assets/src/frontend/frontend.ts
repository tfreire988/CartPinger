/**
 * WhatsCom frontend entry point.
 *
 * TODO v1.0: chat widget open/close interactions, floating button animation.
 */

document.addEventListener( 'DOMContentLoaded', () => {
	const widget = document.getElementById( 'whatscom-chat-widget' );
	if ( ! widget ) return;

	// TODO v1.0: implement widget toggle logic.
} );
